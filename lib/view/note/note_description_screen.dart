import 'package:flutter/material.dart';

class NoteDescriptionScreen extends StatelessWidget {
  const NoteDescriptionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
