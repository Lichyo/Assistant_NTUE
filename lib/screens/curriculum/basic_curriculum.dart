import 'package:flutter/material.dart';

class BasicCurriculum extends StatelessWidget {
  const BasicCurriculum({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
